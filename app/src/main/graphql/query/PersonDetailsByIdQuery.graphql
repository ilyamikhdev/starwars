query PersonDetailsByIdQuery($personId: ID) {
    person(id: $personId) {
        birthYear
        eyeColor
        gender
        hairColor
        homeworld {
            id
            name
            population
        }
        id
        name
    }
}