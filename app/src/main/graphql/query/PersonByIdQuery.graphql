query PersonByIdQuery($personId: ID) {
    person(id: $personId) {
        id
        name
        homeworld {
            id
            name
            population
            gravity
            orbitalPeriod
            rotationPeriod
            surfaceWater
        }
    }
}